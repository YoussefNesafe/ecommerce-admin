import { CustomButton } from '@/components/ui/CustomButton'
import Image from 'next/image'

export default function Home() {
  return (
    <main>
      <CustomButton variant={'outline'} size={'lg'}> Test </CustomButton>
    </main>
  )
}
